这里需要明确指出：提取结果的数据形状已经定好，但下面这些实现规则还没有完全冻结：
* “句子”如何切分，是否使用 Intl.Segmenter，缩写和标点如何处理。
* “附近段落”具体取当前段落、前后各一段，还是按字符窗口。
* webContent 是否排除导航、页脚、隐藏文本、代码块等。
* iframe、Shadow DOM、SPA 页面切换如何处理。
* 正文超过 2000 字符时，是取页面开头，还是优先取选区附近内容。
