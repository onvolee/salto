# Target Chrome first with WXT browser boundaries

Salto MVP will validate and ship first for Chrome and Chromium browsers. Implementation should still use WXT's browser API boundary where practical and avoid scattering direct Chrome-only assumptions through core extension code. Firefox, Safari, and full cross-browser QA are deferred until the Chrome learning and reading loop is proven.
