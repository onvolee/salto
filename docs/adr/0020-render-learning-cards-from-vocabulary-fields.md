# Render learning cards from vocabulary fields

Salto MVP learning cards will store their vocabulary item, card type, and source field keys, then render front and back content from current vocabulary fields. Cards will not duplicate full front/back text snapshots in v0.1. This keeps enrichment retries and field updates naturally reflected in review UI while leaving room for future card-specific overrides if user editing becomes part of the product.
