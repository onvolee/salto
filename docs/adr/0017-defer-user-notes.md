# Defer user notes

Salto MVP will not include a `note` vocabulary field because field values are read-only in v0.1. Keeping a user-sourced note field that users cannot edit would create empty data and misleading UI. Notes can be added later as a deliberate editing feature with clear overwrite and sync-conflict semantics.
