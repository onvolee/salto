# Keep vocabulary field schema fixed for MVP

Salto MVP will use a fixed syncable vocabulary field schema, fixed field display behavior, and read-only vocabulary field values. Users cannot hide, add, delete, redefine, edit, or change the source of vocabulary fields in v0.1. This keeps browser extension and future mobile clients aligned on the same learning data contract while query-template customization remains limited to reading-time translation output.
