# PROTOTYPE - wipe me

Question answered by this prototype:

Does the current Salto domain model support the intended product shape without drifting from the MVP?

What to inspect:

- Reading-time selection translation and save flow.
- Vocabulary enrichment status and generated fields.
- Learning card generation from vocabulary data.
- Browser extension review flow.
- Future mobile consumption of the same syncable records.
- Raw state for `VocabularyItem`, `VocabularyField`, `VocabularyContext`, `LearningCard`, `LearningState`, `ReviewLog`, and `EnrichmentJob`.

Run:

```bash
open .scratch/browser-selection-translation/prototype-web/index.html
```

This is throwaway prototype code. Once the data model and product shape are validated, delete this folder or turn the useful decisions into the PRD/ADR docs.
